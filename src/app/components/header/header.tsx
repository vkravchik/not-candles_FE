"use client";

import styles from "./header.module.css";

const Header = () => {
    return (
        <section
            className={`m-auto mb-12 flex h-screen items-center justify-center bg-cover bg-fixed bg-center ${styles.header}`}
        >
            <div className="rounded-xl bg-purple-300 bg-opacity-50 p-5 text-2xl text-white">
                hjhjhj
            </div>
        </section>
    );
};

export default Header;
